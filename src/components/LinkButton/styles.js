import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
  root: {
    minWidth: '250px',
  },
  linkStyle: { textDecoration: 'none', color: 'white', maxWidth: '500px' },
}))
