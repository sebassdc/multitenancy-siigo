export const userInfo = ({ auth }) => auth.userInfo
export const isUserLoggedIn = ({ auth }) => auth.isUserLoggedIn
export const authError = ({ auth }) => auth.error
export const isAuthInProgress = ({ auth }) => auth.isAuthInProgress
